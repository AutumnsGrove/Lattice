# Local development environment variables
# Copy this file to .dev.vars and fill in actual values
# .dev.vars is gitignored and should never be committed

# GitHub Personal Access Token with 'repo' scope (for private repos)
GITHUB_TOKEN=ghp_your_github_token_here

# Anthropic API Key for Claude
ANTHROPIC_API_KEY=sk-ant-your_anthropic_key_here

# Token Encryption Key for encrypting API tokens in D1 database
# Generate with: openssl rand -hex 32
# This is a 256-bit key (64 hex characters)
TOKEN_ENCRYPTION_KEY=your_64_character_hex_key_here
