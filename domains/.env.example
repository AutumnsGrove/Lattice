# GroveAuth Configuration
# Required for authentication integration with GroveAuth service

# Your GroveAuth client ID (obtain from GroveAuth admin panel)
GROVEAUTH_CLIENT_ID=your-client-id

# Your GroveAuth client secret (set via: wrangler secret put GROVEAUTH_CLIENT_SECRET)
# NEVER commit the actual secret - use wrangler secret for production
# GROVEAUTH_CLIENT_SECRET=your-client-secret

# OAuth callback URL (must match registered redirect URI in GroveAuth)
GROVEAUTH_REDIRECT_URI=https://your-domain.com/auth/callback

# GroveAuth server URL (defaults to https://auth.grove.place)
GROVEAUTH_URL=https://auth.grove.place

# Database Configuration
# Cloudflare D1 database binding name (configured in wrangler.toml)
# DB=groveengine-db

# Optional: Subscription cache TTL in milliseconds (default: 300000 = 5 min)
# SUBSCRIPTION_CACHE_TTL=300000

# Instructions:
# 1. Copy this file to .env for local development
# 2. For production secrets, use: wrangler secret put <SECRET_NAME>
# 3. Never commit .env files with real credentials
# 4. Ensure .env is in .gitignore
#
# Required wrangler secrets for production:
# - GROVEAUTH_CLIENT_SECRET
#
# See COMMANDS_LATER.md for all deployment commands
