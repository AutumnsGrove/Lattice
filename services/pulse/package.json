{
	"name": "grove-pulse",
	"version": "1.0.0",
	"private": true,
	"description": "Webhook receiver for live development heartbeat data from GitHub",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"tail": "wrangler tail",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"dependencies": {
		"zod": "^4.3.6"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20251128.0",
		"typescript": "^5.7.0",
		"vitest": "^2.1.0",
		"wrangler": "^4.63.0"
	}
}
