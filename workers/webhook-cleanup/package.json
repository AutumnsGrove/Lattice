{
  "name": "grove-webhook-cleanup",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker for scheduled webhook cleanup (120-day retention)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251128.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.63.0"
  }
}
