{
	"name": "grove-reverie-exec",
	"version": "0.1.0",
	"private": true,
	"description": "Execution worker — applies Reverie's validated changes via SvelteKit API endpoints",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"tail": "wrangler tail",
		"test": "vitest",
		"test:run": "vitest run",
		"lint": "tsc --noEmit",
		"check": "tsc --noEmit"
	},
	"dependencies": {
		"@autumnsgrove/lattice": "workspace:*",
		"hono": "^4.11.9",
		"zod": "^3.24.0"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.13.3",
		"@cloudflare/workers-types": "^4.20260317.1",
		"typescript": "^5.9.3",
		"vitest": "^4.1.0",
		"wrangler": "^4.76.0"
	}
}
