{
	"name": "grove-warden",
	"version": "0.1.0",
	"private": true,
	"description": "External API gateway — secure credential injection for agents",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"tail": "wrangler tail",
		"test": "vitest",
		"test:run": "vitest run"
	},
	"dependencies": {
		"@autumnsgrove/infra": "workspace:*",
		"@autumnsgrove/lattice": "workspace:*",
		"hono": "^4.11.9",
		"zod": "^3.24.0"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20260210.0",
		"typescript": "^5.9.3",
		"vitest": "^4.1.0",
		"wrangler": "^4.63.0"
	}
}
