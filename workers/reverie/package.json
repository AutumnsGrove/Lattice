{
	"name": "grove-reverie",
	"version": "0.1.0",
	"private": true,
	"description": "Natural language configuration — turns feelings into coordinated API calls across 32 domains",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"tail": "wrangler tail",
		"test": "vitest",
		"test:run": "vitest run",
		"lint": "tsc --noEmit",
		"check": "tsc --noEmit"
	},
	"dependencies": {
		"@autumnsgrove/infra": "workspace:*",
		"@autumnsgrove/lattice": "workspace:*",
		"hono": "^4.7.0",
		"zod": "^3.24.0"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.12.11",
		"@cloudflare/workers-types": "^4.20251128.0",
		"typescript": "^5.7.0",
		"vitest": "^3.2.4",
		"wrangler": "^4.63.0"
	}
}
