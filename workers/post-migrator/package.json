{
  "name": "grove-post-migrator",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker for hot/warm/cold storage migration (cron-triggered)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.11",
    "@cloudflare/workers-types": "^4.20260210.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.64.0"
  }
}
