name: Deploy Ivy

on:
  push:
    branches: [main]
    paths:
      - "apps/ivy/**"
      - "libs/engine/**"
  workflow_dispatch:

permissions:
  contents: read
  deployments: write

jobs:
  deploy:
    uses: ./.github/workflows/_deploy-worker.yml
    with:
      worker-dir: apps/ivy
      needs-engine: true
      needs-foliage: true
      run-build: true
    secrets: inherit
